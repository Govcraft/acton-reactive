use std::any::TypeId;

// type OnStartingHandler = Box<dyn Fn(&IdleActor) + Send + Sync>;
// type UserMessageHandler = Box<dyn Fn(&mut StartedActor, &dyn UserMessage) + Send + Sync>;
// type UserMessageHandlerMap = DashMap<TypeId, UserMessageHandler>;
